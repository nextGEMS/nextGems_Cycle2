sources:
  tco3999-ng5:
    description: 2.8km experiment, coupled with FESOM
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco3999-ng5-concat.yaml"
  tco2559-ng5:
    description: 4km experiment, coupled with FESOM
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco2559-ng5-concat.yaml"
  tco1279-orca025:
    description: 9km baseline, coupled to NEMO, deep conv ON
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco1279-orca025-concat.yaml"
  tco1279-orca025-nodeep:
    description: 9km baseline, coupled to NEMO, deep conv OFF
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco1279-orca025-nodeep.yaml"        
  tco3999-ng5-delete:
    description: 2.8km experiment, coupled with FESOM; not concatenated
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco3999-ng5.yaml"
  tco2559-ng5-delete:
    description: 4km experiment, coupled with FESOM; not concatenated
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco2559-ng5.yaml"
  tco1279-orca025-delete:
    description: 9km baseline, coupled to NEMO, deep conv ON; not concatenated
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco1279-orca025.yaml"        
