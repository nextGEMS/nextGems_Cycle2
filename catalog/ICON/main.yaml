sources:
  ngc2009:
    description: 5km coupled run, main ICON nextGEMS C2 run
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/ngc2009.yaml"
  grids:
    description: ICON grids
    driver: yaml_file_cat
    args:
        path: "/home/m/m300827/catalogs/grids/icon_grids_by_uuid.yaml"
